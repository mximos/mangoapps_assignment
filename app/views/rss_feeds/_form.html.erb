<div class="row">
  <div class="col-lg-6">
    <%= form_for(@rss_feed, :role => "form") do |f| %>
      <% if @rss_feed.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@rss_feed.errors.count, "error") %> prohibited this rss_feed from being saved:</h2>

          <ul>
          <% @rss_feed.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-group">
        <%= f.label :url %><br>
        <%= f.text_field :url, class: "form-control" %>
        <p class="help-block">http://example.com/rss</p>
      </div>
      <div class="form-group">
        <%= f.submit 'Submit', class: "btn btn-primary" %>
        <%= link_to 'Back', rss_feeds_path, class: "btn btn-default" %>
      </div>
    <% end %>
  </div>
</div>